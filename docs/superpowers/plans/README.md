# Plans

Implementation plans produced by writing-plans sessions.

Naming: `YYYY-MM-DD-<topic>-implementation.md`

Each plan is derived from an approved spec and broken into tasks small enough for a subagent to execute independently.
